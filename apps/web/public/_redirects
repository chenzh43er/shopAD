# Cloudflare Pages：没有顶层 404.html 时默认按 SPA 处理（未知路径回退到 /）。
# 缺失的哈希资源：用 public/assets/404.html（目录级 404），避免回退成 index.html
# 导致动态 import 报 “error loading dynamically imported module”。
# 注意：Pages 的 _redirects 不支持 `... 404` 重写，勿再写 /assets/* → 404.html。

# 显式 SPA fallback（与默认行为一致；/api/* 由 Pages Functions 处理）
/*    /index.html   200
